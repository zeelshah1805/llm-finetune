# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# Model artifacts (large; push adapters to HF Hub instead of committing)
adapters/
merged_model/
*.safetensors
*.bin
*.gguf
checkpoints/
outputs/

# Eval outputs: ignore bulky raw prediction dumps, but TRACK the comparison
# table + charts + loss curve — they're the portfolio artifacts shown in the README.
results/raw/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Keep the frozen dataset splits tracked — do NOT ignore data/*.jsonl
